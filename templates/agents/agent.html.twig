
<!-- agents -->
 <!-- agents -->

 {% for user in users  %}   

     {% if 'ROLE_ADMIN' in user.roles %}
 
        <div class="row">
        <div class="col-lg-2 col-sm-2 "><a href="#">
        {# <img src="{{ vich_uploader_asset(user, 'imageFile')}}" alt="card-img-top" style="width: 100%; height:auto;" > #}


        <img src="{{asset('images/properties/' ~ app.user.filename) }}" style="height: 80px" class="img-circle" alt="card-img-top" >

        </a></div>


        <div class="col-lg-6 col-sm-6 "><h4>{{user.name}}</h4><p>Collaboratively administrate empowered markets via plug-and-play networks. Dynamically procrastinate B2C users after installed base benefits. Dramatically visualize customer directed convergence without revolutionary ROI.</p></div>
        <div class="col-lg-4 col-sm-4 "><span class="glyphicon glyphicon-envelope"></span> <a href="mailto:abc@realestate.com">{{user.email}}</a><br>
        <span class="glyphicon glyphicon-earphone"></span>  <br>
        <a href="{{path('calendar_new')}}"><button class="btn btn-success">prendre rendez-vous</button></a>
        </div>
        </div>
        {% endif %}
<!-- agents -->
{% else %}
            <tr>
                <td colspan="11">no records found</td>
            </tr>
{% endfor %}




    {# <tr>
        <td>{{ user.id }}</td>
        <td>{{ user.email }}</td>
        <td>{{ user.roles ? user.roles|json_encode : '' }}</td>

        <td>{{ user.name }}</td>
        <td>{{ user.surname }}</td>
        <td><img src="{{'images/properties/' ~ user.filename }}" height="40"></td>
        <td>{{ user.status }}</td>

        <td>
            <a href="{{ path('admin_user_show', {'id': user.id}) }}" class="btn btn-block btn-info">Show</a>
        </td>
        <td>
            <a href="{{ path('admin_user_edit', {'id': user.id}) }}" class="btn btn-block btn-info">Edit</a>
        </td>
        <td>
            {{ include('admin/user/_delete_form.html.twig') }}
        </td>

    </tr> #}
